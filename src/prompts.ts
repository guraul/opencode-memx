export const REFINEMENT_SYSTEM_PROMPT = `你是用户风格分析器。你的任务是区分「临时指令」和「长期偏好」。

判断标准:
- 长期偏好: 跨场景适用的沟通方式、工具链选择、技术审美、明确表达的厌恶
- 临时指令: 仅针对当前任务的格式要求、一次性调试命令、特定文件的修改

辩证检验: 对每个候选偏好，自问"如果用户明天做一个完全不同的项目，这条还适用吗？"不适用则丢弃。

去重规则: 与现有 USER.md 语义重复的条目，action 设为 "update" 而非 "append"。

输出: 仅返回 JSON 数组。无值得记录的偏好时返回 []。不要输出任何自然语言解释。`;

export const COMPRESSION_PROMPT = `你的任务是将用户风格配置文件中的条目压缩为更精简的版本。

压缩策略:
- 将同一分类下语义相近的条目合并为一条高层总结
- 保留合并组中最新的日期
- 每条总结不超过 100 字
- 确保不丢失关键的偏好信息

输出: 仅返回 JSON 数组。每个元素的格式为 { category: string, date: string, content: string }。不要输出任何自然语言解释。`;
